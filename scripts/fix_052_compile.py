from pathlib import Path

# Kept for workflow compatibility. LABUDA 1.0.0.0 source is now maintained directly
# in the repository; this legacy migration must not overwrite the current UI.
print("LABUDA 1.0.0.0: legacy compile migration skipped")
